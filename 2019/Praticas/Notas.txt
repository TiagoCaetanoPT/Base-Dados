==================================================================
				SINTAX PESQUISAS SQL
==================================================================

SELECT [DISTINCT] <nomes_colunas>
FROM <nome_tabela>
WHERE condição
	[AND|OR...]
ORDER BY <nome_coluna> [ASC|DESC]
;